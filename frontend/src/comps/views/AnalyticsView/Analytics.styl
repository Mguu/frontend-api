.header {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    margin-top: 20px;
    width 100%
    height 120px
}

.titleWrapper {
    margin-left: 80px;
}

.text {
    margin-left: 80px;
}

.pTitle {
    normalArialFont(14px)
    opacity: 0.5;
    color: black;
}

.pText {
    normalArialFont(16px)
    font-weight: bold;
    color: black;
}